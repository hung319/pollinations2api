# .env
# Đây là key để client (App của bạn) gọi vào Proxy
PROXY_API_KEY=sk-my-secret-password-123456

# Cấu hình Port (tùy chọn)
PORT=3000

# API key for upstream service (optional - if you want to use your own pollinations API key)
UPSTREAM_API_KEY=your-pollinations-api-key-here